.shadow {
    display: none;
    position: fixed;
    z-index: 9;
    top:0;
    left:0;
    width: 100%;
    height: 100%;
    background-color: #000;
    opacity: 0.7;
}

.form--active + .shadow,
.comment-form--active + .shadow {
    display: block;
}