import R from 'ramda'

export const getClientById = (state, id) => R.prop(id, state.clients)

export const getClients = (state, ownProps) => {

    const clients = R.map(id => getClientById(state, id), state.registerPage.ids)

    return clients
}